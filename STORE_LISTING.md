# Chrome Web Store掲載文案

## ストア説明文

Amazon Search Filterは、Amazonの商品名規約（ブランド名 ＋ 商品名 ＋ 仕様・型番・カラー・サイズ）に沿わない出品を検索結果で非表示にして、非正規品・模倣品を避けて商品を探しやすくするChrome拡張機能です。商品名の先頭が「令和最新版」などの宣伝文句・装飾記号・年号アピールの出品や、スーパーコピー・レプリカ・並行輸入といった禁止ワードを含む出品が対象です。非表示にした件数はページ内パネルに表示され、「すべて表示」でいつでも元に戻せます。

Amazonのページには折りたためる操作パネルが表示され、拡張機能アイコンを押さなくても、その場で条件を変えて検索・再検索できます（パネルは設定でオフにできます）。カテゴリ（化粧品・ブランド品・家電・サプリメント・スポーツ用品・玩具・時計など）を選ぶと、そのカテゴリ向けの禁止ワードも対象に加わります。非表示ではなく注意ラベルだけを付ける設定にもできます。「正規品」「国内正規品」などのワードを検索語に付け加えることも可能です。

従来どおり、Amazonが発送する商品・Amazon直販商品・割引率での絞り込みも指定できます。選んだ条件はChromeのストレージに保存され、次回も引き継がれます。すべての機能を無料で利用でき、気に入った場合はポップアップから開発者を任意に支援できます（Buy Me a Coffee の外部ページへ移動します）。

検索はAmazon.co.jpの検索URLを生成して新しいタブで開きます。商品情報の収集や外部サーバーへの送信は行いません。本拡張機能はAmazonアソシエイト・プログラムの参加者であり、ユーザーが検索欄から検索を実行したときに生成するURLにアソシエイトタグを付与します。

## 短い説明文

商品名がAmazonの表記規約に沿わない出品（先頭が「令和最新版」など）を検索結果で非表示にします。

## 権限の説明（審査用）

- `storage`: ポップアップで選択した検索条件（正規品クエリ設定・カテゴリ・絞り込み設定）を保存し、次回起動時に復元するために使用します。外部送信は行いません。
- コンテンツスクリプト（`https://www.amazon.co.jp/` 配下）: どのAmazonページでも操作パネルを表示し、検索結果ページでは商品タイトルと拡張機能内の除外ワード一覧を照合して一致した出品を非表示（または注意ラベル表示）にするために使用します。ページDOMの読み取り・表示変更のみで、データの収集・送信は行いません。パネルはポップアップの設定で非表示にできます。
- `host_permissions` は宣言していませんが、`content_scripts` の `matches` により Chrome ウェブストア上では「host permission」として扱われ、理由の入力が必要になります。

## Developer Dashboard 提出時の入力内容（v1.3.0 で実際に提出したもの）

アップデート提出時も同じ項目を聞かれるため、そのまま再利用する。

### storage 権限を要求する理由

ユーザーがポップアップまたはページ内パネルで選択した設定（正規品フィルタのオン/オフ、正規品ワードの検索語への付加、商品カテゴリ、Amazon発送・Amazon直販・割引率の絞り込み、パネルの表示可否）を保存し、次回起動時に復元するために chrome.storage.sync を使用します。保存するのはユーザーが選択した設定値のみで、検索キーワードや閲覧履歴は保存しません。外部サーバーへの送信は一切行いません。

### ホストに対して権限を要求する理由

本拡張機能は host_permissions を宣言しておらず、コンテンツスクリプトの対象を https://www.amazon.co.jp/* に限定しています。このアクセスは次の3点にのみ使用します。(1) Amazonのページ上に、その場で検索条件を変更・再検索できる折りたたみ可能な操作パネルを表示する。(2) 検索結果ページ（/s）で、表示されている商品タイトルの文字列を、拡張機能内に同梱した禁止ワード一覧および商品名規約の判定ルールと照合し、規約外と判定した出品を非表示または注意ラベル表示にする。(3) パネルの検索欄からユーザーが検索を実行した際、遷移先のAmazon検索URLにAmazonアソシエイト・プログラムのトラッキングタグ（tagパラメータ）を付与する。既存のページや、ユーザーが操作していない自動的な遷移にタグを差し込むことはありません。処理はすべてページ内で完結し、商品情報やユーザーデータの収集・保存・外部送信は行いません。Amazon.co.jp 以外のサイトでは一切動作しません。

### リモートコードの使用

「いいえ、リモートコードを使用していません」を選択。

実行されるJavaScriptはすべて拡張機能パッケージに同梱されています（dictionary.js / popup.js / content.js）。eval() や new Function() は使用せず、外部から取得したスクリプトの読み込み・実行も行いません。唯一の fetch はパッケージ内の parametor.txt の読み込みです。Buy Me a Coffee へのリンクは通常のリンクとして新しいタブでページを開くだけで、コードの取得や実行は行いません。

### データ使用の申告

- 収集するデータの項目: **すべてチェックなし**。設定値は `chrome.storage.sync` にユーザー自身の Chrome/Google アカウント内で保持されるのみで開発者には届かず、商品タイトルはページ内で照合するだけで保存も送信もしないため「収集」に当たらない。
- 表明のチェックボックス3つ（第三者への販売なし／単一目的以外の利用なし／信用調査・融資目的の利用なし）は**すべてチェック**。

### プライバシーポリシー URL

https://maylabstudio-lab.github.io/amazon-search-filter/

### 追加フィールド

- ホームページURL: https://github.com/maylabstudio-lab/amazon-search-filter
- サポートURL: https://github.com/maylabstudio-lab/amazon-search-filter/issues
- 公式URL: 未設定（ドメイン所有権の確認が必要で `github.io` では設定不可）

### EEA 取引業者ステータス

非取引業者（non-trader）として申告。拡張機能は無料で、Buy Me a Coffee は外部サイトでの任意の寄付であり製品の対価ではないため。有料機能を追加する場合は取引業者へ変更が必要。

## 単一の目的（Single purpose）

Amazon.co.jp の検索結果から、商品名がAmazonの表記規約に沿わない出品（非正規品・模倣品の疑いがあるもの）を絞り込み・非表示にすること。

## プライバシーポリシーの要約

本拡張機能は、検索キーワードと選択した条件からAmazon.co.jpの検索URLを生成します。入力内容、検索履歴、商品情報を外部サーバーへ送信・保存しません。選択した条件はChromeのストレージ（`chrome.storage.sync`）にのみ保存します。

本拡張機能はAmazonアソシエイト・プログラムの参加者です。ユーザーが検索欄から検索を実行したときに生成する検索URLにアソシエイトタグ（`tag`パラメータ、ストアID: `maylab-22`）を付与し、このタグ経由の購入について開発者が紹介料を得ることがあります。既存のページへ自動でタグを差し込むことはありません。任意の支援は外部サービス（Buy Me a Coffee）で行われます。Amazonでの閲覧や購入についてはAmazonのプライバシーポリシーと利用規約が適用されます。
